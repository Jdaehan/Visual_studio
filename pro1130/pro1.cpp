//템플릿 예제 - 20191450 장대한
/*
#include<iostream>
using namespace std;

class Circle  //원의 클래스 선언 
{
	//멤버변수
	//멤버함수
};

//템플릿 선언
template <class T>
void myswap(T& a, T& b) //값을 교환하는 기능의 함수
{
	T tmp;
	tmp = a;
	a = b;
	b = tmp;
}

int main()
{
	int a, b;
	cout << "두개의 교환할 정수형 숫자 입력: " << endl;
	cin >> a >> b;
	cout << "swap 함수 호출 전 : a = " << a << ".b =" << b << endl;
	myswap(a, b);
	cout << "swap 함수 호출 후 : a = " << a << ".b =" << b << endl;

	double c, d;
	cout << "두개의 교환할 실수형 숫자 입력: " << endl;
	cin >> c >> d;
	cout << "swap 함수 호출 전 : c = " << c << ".d =" << d << endl;
	myswap(c, d);
	cout << "swap 함수 호출 후 : c = " << c << ".d =" << d << endl;

	char c1, c2;
	cout << "두개의 교환할 문자 입력: " << endl;
	cin >> c1 >> c2;
	cout << "swap 함수 호출 전 : c1 = " << c1 << ".c2 =" << c2 << endl;
	myswap(c1, c2);
	cout << "swap 함수 호출 후 : c1 = " << c1 << ".c2 =" << c2 << endl;


	return 0;
}
*/