//STL Vector 실습 - 20191450 장대한
#include<iostream>
#include<vector>
#include<string>
using namespace std;
/*
int main()
{
	vector<int> v; //벡터 객체 선언
	vector<string> s; // 문자열 입력 가능한 벡터 객체 선언
	string name;
	int num;

	cout << "데이터를 5개 입력해주세요 " << endl;
	for (int i = 0; i < 5; i++)
	{
		cin >> num;
		v.push_back(num);
	}

	cout << "이름 3개 입력해주세요." << endl;
	for (int i = 0; i < 3; i++) // 한줄에 한개씩 이름 입력
	{
		cout << i+1 << ">>";
		cin >> name; // 키보드에서 이름을 입력
		s.push_back(name); // 이름을 벡터에 삽입
	}

	cout << "입력한 이름과 숫자를 출력합니다." << endl;
	for (int i = 0; i < s.size(); i++)
	{
		cout << s[i] << endl;
	}

	for (int i = 0; i < v.size(); i++)
	{
		cout << v[i] << " ";
	}

	return 0;
} */