//STL Vector, iterator 실습 - 20191450 장대한
#include<iostream>
#include<vector>
#include<string>
using namespace std;
/*
int main()
{
	vector<int> v; // 객체 선언
	int num;
	cout << "3개의 정수값 입력: " << endl;
	for (int i = 0; i < 3; i++)
	{
		cin >> num;
		v.push_back(num);
	}

	vector<int>::iterator it; // 벡터 v의 원소에 대한 포인터 it 선언

	for (it = v.begin(); it != v.end(); it++)
	{
		int n = *it;
		n = n * 2;
		*it = n; //it이 가리키는 원소에 값을 쓰기
		cout << "바뀐 *it의 값 = " << *it << endl;
	}

	for (it = v.begin(); it != v.end(); it++)
	{

	}

	return 0;
}*/